# Google OAuth credentials for scripts/getRefreshToken.js
# Create a real .env (gitignored) with your actual values.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: keep the browser from opening automatically
# NO_OPEN=1
